from setuptools import setup, find_packages

setup(
    name="pololu_ticcmd",
    version="0.1.0",
    packages=find_packages(),
    entry_points={
        'console_scripts': [
        ],
    },
    install_requires=[
    ],
    extras_require={
    },
)